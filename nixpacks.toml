[phases.setup]
nixPkgs = ['bun', 'curl']

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "chmod +x ./scripts/build.sh && ./scripts/build.sh"
]

[start]
cmd = "./scripts/prestart.sh & bun run start"